<mongoChangeLog>
    <changeSet changeId="ChangeSet-9" author="stepan_litvinov">
        <script>

            db.component.remove({});
            db.experiment.remove({});
            db.notebook.remove({});
            db.project.remove({});
            db.template.remove({});
            db.sequence_id.remove({});

            db.sequence_id.insert({"_id" : "experiment", "seq" : 0 });
            db.sequence_id.insert({"_id" : "notebook", "seq" : 0 });
            db.sequence_id.insert({"_id" : "project", "seq" : 0 });
            db.sequence_id.insert({"_id" : "template", "seq" : 0 });

        </script>
    </changeSet>
</mongoChangeLog>