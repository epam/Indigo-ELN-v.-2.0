version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto21
  pre_build:
    commands:
      - env
      - aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $REGISTRY_URI
      - IMAGE_TAG=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
  build:
    commands:
      - cd backend
      - echo XXX1
      - "bash -c 'ip addr' | true"
      - echo XXX2
      - "bash -c 'ping host-gateway' | true"
      - echo XXX3
      - "bash -c 'ping host.docker.internal' | true"
      - ./gradlew build :integrationTests:quarkusIntTest --info
  post_build:
    commands:
      - docker tag indigoeln/eln-lambda:built $ELN_REPO_URI:latest
      - docker tag indigoeln/eln-lambda:built $ELN_REPO_URI:$IMAGE_TAG
      - docker push $ELN_REPO_URI:latest
      - docker push $ELN_REPO_URI:$IMAGE_TAG
reports:
  unit_tests:
    files:
      - backend/**/build/test-results/test/TEST-*.xml
    file-format: JUNITXML
  integration_tests:
    files:
      - backend/**/build/test-results/quarkusIntTest/TEST-*.xml
    file-format: JUNITXML
